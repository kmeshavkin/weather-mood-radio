import styled from 'styled-components';
import { Grid } from '@material-ui/core';

export const StatusBar = styled(Grid)`
  margin: auto;
`;

export const PlayerGridStyled = styled(Grid)`
  margin: auto;
`;
